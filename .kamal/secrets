# Org-level secrets (shared across all tastybamboo apps on Giant)
KAMAL_REGISTRY_PASSWORD=$GIANT_KAMAL_REGISTRY_PASSWORD

# App-level secrets (repo secrets, no prefix)
RAILS_MASTER_KEY=${RAILS_MASTER_KEY:-$(cat config/master.key 2>/dev/null)}
DATABASE_URL=$DATABASE_URL
GITHUB_CLIENT_ID=$OAUTH_GITHUB_CLIENT_ID
GITHUB_CLIENT_SECRET=$OAUTH_GITHUB_CLIENT_SECRET
